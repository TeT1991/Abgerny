using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BtnID : MonoBehaviour
{
    private string ID;

    // Start is called before the first frame update
    void Start()
    {
        ID = gameObject.name;
        //print(ID);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
